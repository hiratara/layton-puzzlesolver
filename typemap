TYPEMAP 
Layton_SlidePuzzle_State *                LAYTON_STATE

OUTPUT 
LAYTON_STATE
    sv_setref_pv($arg, \"Layton::SlidePuzzle::State\", (void *) $var);

INPUT 
LAYTON_STATE
    $var = INT2PTR( $type, SvIV( (SV *) SvRV($arg) ) ); 




TYPEMAP 
Layton_SlidePuzzle_Board *                LAYTON_BOARD

OUTPUT 
LAYTON_BOARD
    sv_setref_pv($arg, \"Layton::SlidePuzzle::Board\", (void *) $var);

INPUT 
LAYTON_BOARD
    $var = INT2PTR( $type, SvIV( (SV *) SvRV($arg) ) ); 
